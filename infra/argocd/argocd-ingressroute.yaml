apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: argocd
  namespace: argocd
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`argocd.memoire.local`)
      kind: Rule
      services:
        - name: argocd-server
          namespace: argocd
          port: 80
