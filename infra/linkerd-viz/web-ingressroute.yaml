apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: linkerd-viz
  namespace: linkerd-viz
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`linkerd.memoire.local`)
      kind: Rule
      services:
        - name: web
          namespace: linkerd-viz
          port: 8084
