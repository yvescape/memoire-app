apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: prometheus
  namespace: monitoring
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`prometheus.memoire.local`)
      kind: Rule
      services:
        - name: kube-prometheus-stack-prometheus
          namespace: monitoring
          port: 9090
