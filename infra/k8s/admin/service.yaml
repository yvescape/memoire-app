apiVersion: v1
kind: Service
metadata:
  name: admin
  namespace: memoire-app
  labels:
    app: admin
spec:
  selector:
    app: admin
  ports:
    - name: http
      port: 3001
      targetPort: 80
  type: ClusterIP