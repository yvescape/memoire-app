apiVersion: v1
kind: Service
metadata:
  name: products-service
  namespace: memoire-app
  labels:
    app: products-service
spec:
  selector:
    app: products-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  type: ClusterIP